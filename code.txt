class A {
  static function divide(x, y) {
    if (y == 0)
      throw y;
    return x / y;
  }

  static function main() {
    var x;
    var y;

    try {
     x = divide(5, 0);
    }
    catch(e) {
      x = e;
    }
    finally{
	x = x * 2;
}
    return x;
  }
}